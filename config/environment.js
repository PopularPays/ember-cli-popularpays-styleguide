'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'ember-cli-popularpays-styleguide',
    // namespaced directory where resolver will look for your resource files
    podModulePrefix: 'ember-cli-popularpays-styleguide/pods',
    environment: environment,
    rootURL: '/',
    locationType: 'auto'
  };

  return ENV;
};
